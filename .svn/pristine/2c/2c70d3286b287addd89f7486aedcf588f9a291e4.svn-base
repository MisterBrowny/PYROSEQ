#include "includes.h"


void delay_ms (word TimeToWait)
{
	word temp = Cptms;

	while(TempsInf(temp,TimeToWait));
}

void delay_s (word TimeToWait)
{
	word temp1 = Cptms;
	word temp2 = TimeToWait * 1000;

	while(TempsInf(temp1,temp2));
}